package enums;

public class Declaration {
	
	 public enum WeekDays{                      //Allowed inside & outside(obviously) class
		 SUN, MON, TUE, WED, THU, FRI, SAT
	 }
	
	public void testMethod() {
		/*public enum Days{                      //Not allowed inside method
			 SUN, MON, TUE, WED, THU, FRI, SAT
		}*/
	}
}
